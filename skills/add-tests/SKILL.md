---
name: add-tests
description: Add new revv QA tests to cover local changes. Analyzes git differences (e.g. diff main...HEAD or HEAD~1) and decides if new tests are required. Creates structured test markdown files if needed. Trigger on 'revv add-tests', 'add new tests', 'create new tests', 'add test coverage'.
---

Read `prompt.md` in this directory and follow the instructions.
