---
name: update-repo
description: Update existing revv QA tests. Scans the existing `.revv/` directory, checks the last commit history, and synchronizes test definitions by updating or deleting outdated ones and proposing necessary modifications. Trigger on 'revv update', 'update tests', 'refresh tests', 'sync tests'.
---

Read `prompt.md` in this directory and follow the instructions.
